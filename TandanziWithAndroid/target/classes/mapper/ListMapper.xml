<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scit.tandanzi.dao.ListMapper">

<!-- 검색 -->
      <select id="getFoodList" parameterType="String" resultType="SlList">
         select * from simpleList
         where slType=#{slType}
      </select>
</mapper>